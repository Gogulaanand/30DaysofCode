
/*

Notes section:

imperative => a set of commands for the computer to perform a task

first order function => functions that are passes into, returned from other functions or assigned to variables

lambda function => a first order function

higher order function => function that accepts or returns functions


Principles of functional prog:
    - declare funtion arguments and pass them when used to avoid confusion later on incase global avr are being accessed
    - do ot alter global var, use local ones

*/

